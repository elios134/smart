model Notification {
  id        Int      @id @default(autoincrement())
  type      String // 'production' | 'vente'
  message   String   @db.Text
  createdAt DateTime @default(now())

  @@index([createdAt])
  @@map("notifications")
}
