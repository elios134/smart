model Session {
  sid       String   @id
  data      String   @db.Text
  expiresAt DateTime

  // Accélère le nettoyage périodique des sessions expirées (clearExpired)
  @@index([expiresAt])
  @@map("sessions")
}
